# Copy to .env for local runs, or set these as Replit Secrets in deployment.
# Never commit a filled-in .env — it is covered by .gitignore.
HUBSPOT_CLIENT_ID=
HUBSPOT_CLIENT_SECRET=
HUBSPOT_REDIRECT_URI=
