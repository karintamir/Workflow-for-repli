# Secrets — never commit real credentials
.env
.env.*
!.env.example

# hs_audit.py output
audit_out/
*_hs_audit.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Client audit deliverables — contain client CRM data and employee emails.
# Deliver via Drive/email, not git history.
out/
